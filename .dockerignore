# node_modules はホスト（macOS）のバイナリを含むので、どの階層のものも持ち込まない。
# 先頭階層だけを指す "node_modules" では apps/*/node_modules が漏れる。
**/node_modules
**/dist
.next
.env
.env.*
.git
npm-debug.log
Dockerfile
.dockerignore
playwright-report
test-results
